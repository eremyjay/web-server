

module.exports = {
    // Place receivers here -> serviceName: function definition
    // 'service': function(app, instruction, data, res) { return "received"; }
    // 'service2': function(app, instruction, data, res) { return "received 2"; }
};